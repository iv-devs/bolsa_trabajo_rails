<div class="section lb">
    <div class="container">
        <div class="row">
            <div class="sidebar col-md-4">
                <%= render 'menu_company' %>
            </div><!-- end col -->

            <div class="content col-md-8">
                <div class="post-padding">
                    <div class="job-title nocover hidden-sm hidden-xs"><h5>Postulantes a <%= @job.title %></h5></div>
                    <div class="row">
                        <div class="col-md-12">
                            <div class="table-responsive job-table">
                                <table id="mytable" class="table table-bordred table-striped">

                                    <thead>
                                        <tr>
                                            <th>Nombre del Postulante</th>
                                            <th>Correo</th>
                                            <th>Detalle de postulación</th>
                                        </tr>
                                    </thead>

                                    <tbody>
                                        <% @job.postulations.each do |p| %>
                                           <tr>
                                                <td>
                                                    <h4> <%= p.user.first_name %>  <br>
                                                        <small>Postulo el : <%= p.created_at.strftime(" %d/%m/%Y ") %></small> <small> Nombre de usuario  <%= p.user.username %> </small>
                                                    </h4>
                                                </td>
                                                <td><a href="mailto:<%= p.user.email %>"><%= p.user.email %></a></td>
                                                <td>
                                                    <%= link_to company_details_path(current_company.id, @job.id, p.id) do %>
                                                        <i class="fa fa-eye" aria-hidden="true"></i>
                                                    <% end %>
                                                </td>
                                            </tr>
                                        <% end %>
                                    </tbody>

                                </table>
                            </div><!-- end table -->
                        </div><!-- end col -->
                    </div><!-- end row -->
                </div><!-- end post-padding -->
            </div><!-- end col -->
        </div><!-- end row -->  
    </div><!-- end container -->
</div><!-- end section -->